# Telegram bot (scripts/bot.py)
TELEGRAM_BOT_TOKEN=change-me

# OpenAI (extraction, answer generation, evaluation)
OPENAI_API_KEY=change-me

# Optional: where the FAISS index is stored (default: kb_index)
KB_INDEX_DIR=kb_index
